name: Package Size Report

on:
  pull_request:
    branches: [dev]

jobs:
  pkg-size-report:
    name: Package Size Report
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          registry-url: https://artifactory.wikia-inc.com/artifactory/api/npm/wikia-npm/
          NODE_AUTH_TOKEN: ${{ secrets.IDENTITY_TOKEN }}
          node-version-file: '.nvmrc'

      - name: Package size report for ucp-desktop
        uses: pkg-size/action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ARTIFACTORY_NPM_TOKEN: ${{ secrets.ARTIFACTORY_NPM_TOKEN }}
        with:
          display-size: gzip
          build-command: npm run build:platforms -- --env platform=ucp-desktop
          hide-files: '*.{map,txt}'
          unchanged-files: hide

      - name: Package size report for ucp-mobile
        uses: pkg-size/action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ARTIFACTORY_NPM_TOKEN: ${{ secrets.ARTIFACTORY_NPM_TOKEN }}
        with:
          display-size: gzip
          build-command: npm run build:platforms -- --env platform=ucp-mobile
          hide-files: '*.{map,txt}'
          unchanged-files: hide

      - name: Package size report for f2
        uses: pkg-size/action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ARTIFACTORY_NPM_TOKEN: ${{ secrets.ARTIFACTORY_NPM_TOKEN }}
        with:
          display-size: gzip
          build-command: npm run build:platforms -- --env platform=f2
          hide-files: '*.{map,txt}'
          unchanged-files: hide

      - name: Package size report for ucp-no-ads
        uses: pkg-size/action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ARTIFACTORY_NPM_TOKEN: ${{ secrets.ARTIFACTORY_NPM_TOKEN }}
        with:
          display-size: gzip
          build-command: npm run build:platforms -- --env platform=ucp-no-ads
          hide-files: '*.{map,txt}'
          unchanged-files: hide
